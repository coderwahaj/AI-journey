## Almost same as Broadcasting .
# The difference is Broadcasting expands the element  to match the dimesnion(Apply on 1D to 2D)
# The vectorization doing a single thing on whole elements of array in one go (Apply on Matrix operations)
# Vectorization is 100 times faster than loops